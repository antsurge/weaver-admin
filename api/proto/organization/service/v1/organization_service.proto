syntax = "proto3";

package organization.service.v1;

import "organization/service/v1/department.proto";

service OrganizationService {
  rpc CreateOrganization(CreateDepartmentRequest) returns (Department);
}
