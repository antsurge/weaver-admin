# 2026-07-28 工作日志

## 管理员用户管理：表单增加密码字段 + 后端默认密码逻辑

### 需求
- 表单新增密码字段（InputPassword）
- 创建时若密码为空，后端使用默认密码 `123456`
- 编辑时密码为空不修改，有值时更新

### 修改清单

#### 1. proto - api/proto/identity/service/v1/admin.proto
`UpdateAdminRequest` 新增 password 字段（编号 8，紧跟在 username=7 后）：
```proto
// 密码（可选；空表示不修改；非空表示更新密码）
string password = 8 [(gnostic.openapi.v3.property) = {description: "密码（可选）"}];
```

#### 2. proto 生成代码 - `cd api && buf dep update && buf generate`
- `api/gen/go/identity/service/v1/admin.pb.go` - UpdateAdminRequest 新增 Password 字段（protobuf tag 8）
- `api/gen/openapi/openapi.yaml` - UpdateAdminRequest schema 自动加入 password

#### 3. biz - app/admin/service/internal/biz/admin.go
```go
const AdminDefaultPassword = "123456"

func (uc *AdminUseCase) CreateAdmin(ctx context.Context, admin *Admin) (*Admin, error) {
    // ... ID/时间戳赋值 ...
    if admin.Password == "" {
        admin.Password = AdminDefaultPassword
    }
    // ... 持久化 + 绑定角色 ...
}
```
`UpdateAdmin` 保持不变（密码更新逻辑下沉到 data 层处理"空跳过"）。

#### 4. data - app/admin/service/internal/data/admin.go
`UpdateAdmin` 改为构造 builder，非空才调用 SetPassword：
```go
updateOne := r.data.db.Admin.
    UpdateOneID(admin.ID).
    SetRealName(...).SetUsername(...).SetEmail(...).SetPhone(...).SetAvatar(...).
    SetUpdatedAt(time.Now())

if admin.Password != "" {
    updateOne = updateOne.SetPassword(admin.Password)
}

_, err := updateOne.Save(ctx)
```
`CreateAdmin` 保持不变（biz 层已兜底默认密码）。

#### 5. 前端 - frontend/apps/web-antd/src/api/adminuser/admin.ts
`Admin` 接口添加 `password?: string` 字段及 JSDoc 说明。

#### 6. 前端 - frontend/apps/web-antd/src/views/adminuser/admin/modules/form/index.vue
- schema 新增密码字段（VbenInputPassword），placeholder 提示用户
  "编辑时留空则不修改；创建时留空使用默认密码 123456"
- 编辑模式：`formApi.setValues(res)` 不会填入 password（后端 API 不返回 password 字段，Sensitive 标签 JSON 序列化跳过）
- 提交：`getValues()` 返回 `password: ''` 时后端自动跳过；创建时空字符串由 biz 层兜底

### 验证
- `go build ./...` (app/admin/service) 通过
- 前端 typecheck 有大量既有的非相关 TS 错误（sidebar.vue、preferences.vue、use-vxe-grid.ts、generate-menus.ts），与本次改动无关

### 关键知识点
- **Vben Form 密码组件**：`component: 'VbenInputPassword'`（不是 `'InputPassword'`，参考 `views/_core/authentication/login.vue` 和 `views/_core/profile/password-setting.vue`）
- **ent SetPassword 跳过策略**：用 builder 模式累加 SetXxx，最后判断是否调用 SetPassword，避免每次 update 都覆盖密码
- **biz/data 分工**：biz 层处理默认值（创建时默认密码），data 层处理条件更新（编辑时空跳过），各司其职
- **proto 字段编号**：UpdateAdminRequest 现有字段 1-7 + 20，password 分配编号 8；20 留给 roleIds（业务约定）
- **Sensitive 字段**：ent schema `field.String("password").Sensitive()` 标签让 JSON 序列化跳过 password，
  前端 GetAdmin API 不会收到 password 字段，编辑表单密码框保持空白（安全）

---

## 菜单管理：按钮类型新增"接口权限"字段（方案确认，待实施）

### 需求
菜单类型 = `action`（按钮）时，可绑定多个 protobuf 定义的接口权限。

### 用户已确认的 5 个设计决策
1. **存储方式**：新建关联表（menu_api_permissions 表，规范化、支持反查）
2. **接口元数据加载**：方案 A - 后端服务启动时扫描 openapi.yaml 并提供内部接口
   `GET /admin/v1/api-metadata` 返回按 service 分组的接口列表
3. **MenuTree 响应携带 apiPermissions**：带，角色授权页能展示该按钮包含哪些接口
4. **菜单列表页是否展示**：不展示（仅列表中存于数据库，UI 不展示）
5. **角色授权细粒度到接口**：本期不实现（v2 再说）

### 实施步骤（待执行）
1. proto 新增 `ApiPermission` message，Menu/CreateMenuRequest/UpdateMenuRequest 增字段
2. ent schema 新建 `menu_api_permissions` 表（edge from Menu）
3. 后端启动扫描 openapi.yaml，注册 internal handler
4. biz/data 层增 ApiPermission 模型和 CRUD
5. 前端新增 ApiPermissionPickerModal（service 树 + 接口列表 + 多选）
6. 表单嵌入选择弹窗入口，仅 action 类型显示
7. 验证：proto generate、go generate ent、go build、前端 typecheck

---

## 菜单管理接口权限（续）：后端完成

### 已完成（截至本次会话）
- proto: `api/proto/permission/service/v1/menu.proto` 加 `ApiPermission` + 在 Menu/CreateMenuRequest/UpdateMenuRequest 加 `repeated ApiPermission apiPermissions` (字段号 21/18/19)
- proto: `api/proto/permission/service/v1/api_metadata.proto` 新建，定义 `ApiMetadata/ApiEndpoint/ListApiMetadataRequest/Response`
- proto: `api/proto/admin/service/v1/i_permission.proto` 加 `rpc ListApiMetadata` → `GET /admin/v1/api-metadata`
- ent: 新建 `data/ent/schema/api_permission.go`（id/service/method/path/summary/code 唯一业务键 + m2m with Menu）
- ent: `data/ent/schema/menu.go` 加 `edge.To("api_permissions", ApiPermission.Type)`（双向 edge 另一侧在 ApiPermission.From）
- biz: `biz/menu.go` Menu 结构加 `APIPermissions []*ApiPermission`，新增 `ApiPermissionRepo` 接口
- data: `data/api_permission.go` 新建（UpsertByCodes / ListByCodes / ListAll），实现 `biz.ApiPermissionRepo`
- data: `data/menu.go` 重构：构造器接 `biz.ApiPermissionRepo`；CreateMenu/UpdateMenu 用 AddAPIPermissionIDs/ClearAPIPermissions；ListMenu/GetMenusByIDs 加 `WithAPIPermissions()` 预加载；提取 `toBizMenu()` 复用
- scanner: `data/openapi_scanner/scanner.go` 新建，按 service 名分组（operationId 前缀解析 "Identity_ListAdmin" → "IdentityService"）
- data: `data.go` 加 `NewOpenAPIScanner(logger)`，启动时 Scan 一次；加入 ProviderSet
- service: `service/permission.go` 加 `ListApiMetadata` handler
- wire_gen: 同步更新 `apiMetadataScanner` 注入路径

### 顺手修复（与本次任务相关的 build 阻塞项）
- 3 个 `identity.pb*.validate.go` 旧文件残留包名 `identityypb`（应 `identitypb`），用 sed 改回（已备份 `.bak`）
- build 命令需显式设 `GOROOT=/Users/wjf/.gvm/gos/go1.24.11`，否则 PATH 里 1.22.1 抢占导致 GOROOT 指向旧版本（虽然 go version 输出 1.24.11）

### 验证
- `go build ./...` 通过（用绝对路径 `/Users/wjf/.gvm/gos/go1.24.11/bin/go` + 强制 GOROOT）

### 关键经验
- **wire 注入的"具体类型 vs 接口"陷阱**：`data.NewMenuRepo(... *apiPermissionRepo)` 要具体类型时，wire 只能传 `data.NewApiPermissionRepo(...)` 的具体类型；但 `NewApiPermissionRepo` 返回的是 `biz.ApiPermissionRepo` 接口 — 解决：把 `NewMenuRepo` 改成接 `biz.ApiPermissionRepo` 接口
- **GOROOT 与 go 命令的查找**：`gvm` 把 `go` 软链接到 1.22.1，PATH 优先策略让 `go version` 看着对但 `GOROOT` 是错的；保险做法是 `GOROOT=... ./go ...` 显式设
- **openapi_scanner 包名**：必须用目录名作为 Go 包名（`openapi_scanner`），别用 `scanner`，否则和别的 `scanner` 包冲突

---

## 接口权限前端落地（本次会话）

### 文件改动
- `frontend/apps/web-antd/src/api/permission/menu.ts`：在 `PermissionMenuApi` namespace 内加 `ApiPermission / ApiEndpoint / ApiMetadata` 类型；`PermissionMenu` 加 `apiPermissions?: ApiPermission[]` 字段；新增 `listApiMetadataApi()` 函数
- `frontend/apps/web-antd/src/adapter/component/index.ts`：注册 `ApiPermissionPicker` 自定义表单组件到 `ComponentType` 联合类型和 components 映射表
- `frontend/apps/web-antd/src/components/form/api-permission-picker/index.vue`（新建）：表单控件主体，已选 API 以 `Tag` 展示，点击"新增接口权限"打开 picker modal
- `frontend/apps/web-antd/src/components/form/api-permission-picker/api-permission-picker.vue`（新建）：picker modal，左侧 service 树（可勾选 Tree）+ 搜索框；右侧已选列表（可单独取消）
- `frontend/apps/web-antd/src/views/permission/menu/modules/form/index.vue`：在 schema 中追加 `apiPermissions` 字段，组件为 `ApiPermissionPicker`，依赖 `type === PermissionTypeOptionsValueAction` 才显示
- `frontend/apps/web-antd/src/locales/langs/zh-CN/permission.json`：补 `apiPermissions` 字段名 + `apiPermission` 分组的 9 条文案（pickerTitle/addButton/searchPlaceholder/selectedCount/selectedEmpty/empty/tip/remove）

### 验证
- `go build ./...` 通过
- `pnpm exec vue-tsc --noEmit --skipLibCheck` 总计 79 个 TS 错误；与本次改动相关的只剩：
  - `form/index.vue(2,1) ChangeEvent unused`：line 2 的预存导入，与本次改动无关
  - `api/permission/menu.ts(123) Cannot find name 'MenuTypes'`：上游遗留类型不一致，原文件就报错了，未引入
- 其余 77 个错误均在 adminuser/organization/system/icon-picker/router/effects/* 等**与本次无关**的模块

### 关键经验
- **pnpm 多包结构**：`pnpm run typecheck` 不能在仓库根或 frontend 根运行，必须切到具体 app 包 `frontend/apps/web-antd`，脚本定义在那里
- **Vben 自定义表单组件接入**：三步：(1) 组件 emit `confirm` 等事件；(2) `adapter/component/index.ts` 加 union 类型 `ComponentType`；(3) schema `component` 用字符串名引用
- **ant-design-vue Tree on-check 回调签名**：strict 模式下联级 `Key[] | { checked; halfChecked }` 类型用 type guard `.filter((k): k is string => ...)` 把数组元素收窄
- **Tree 节点 key 设计**：跨 service 同 method+path 可能冲突 → 用 `${service}::${method} ${path}` 拼接保证唯一；`keyOf` 工具函数单独保留纯接口端的 `METHOD PATH`

---

## 接口管理（api_interface）— 完整后端 + 前端

### 背景
openapi.yaml 路径解析修复方案失败（用户确认"现在还是获取不到"），用户要求更换方案：建表 `api_interface` 管理所有 API，通过上传导入 openapi.yaml 文件。功能放在 system 模块下。

### 后端完成清单（上一轮已完成）

#### 1. ent schema — `data/ent/schema/api_interface.go`
- 字段：id(36位UUID)/service/tag/method/path/summary/code(唯一)/created_at/updated_at
- 无 edges
- `ent generate` 通过

#### 2. proto 定义
- `api/proto/system/service/v1/api_interface.proto` — ApiInterface message + CRUD 请求响应
- `api/proto/system/service/v1/system_service.proto` — 空 service 定义
- `api/proto/admin/service/v1/i_system.proto` — 聚合 RPC + HTTP 路由
  - `GET /admin/v1/api-interface` — 分页列表
  - `POST /admin/v1/api-interface/import` — 上传导入（body: file bytes）
  - `DELETE /admin/v1/api-interface` — 批量删除
- `buf generate` 通过

#### 3. biz 层 — `biz/api_interface.go`
- `ApiInterface` 模型，`ListApiInterfaceRequest/Response`，`ImportResult`
- `ApiInterfaceRepo` 接口，`ApiInterfaceUsecase`（List / Import / Delete）

#### 4. data 层
- `data/api_interface.go` — 实现 `ApiInterfaceRepo`：ListApiInterface（分页+模糊搜索）、UpsertApiInterface（按 code 查找→创建/更新）、DeleteApiInterface（批量）
- `data/openapi_importer.go` — 解析 openapi.yaml 内容为 `[]*biz.ApiInterface`，提取 service/tag/method/path/summary/code

#### 5. service 层 — `service/system.go`
- 实现 `adminV1.SystemHTTPServer`，三个 RPC 方法

#### 6. 依赖注入
- `biz.go` / `data.go` / `service.go` — ProviderSet 新增
- `server/http.go` / `server/grpc.go` — 新增 SystemService 参数+注册
- `wire_gen.go` — 手动同步
- `go build ./...` 通过

### 前端完成清单（本轮）

#### 1. API 层 — `api/system/api-interface/index.ts`
- `SystemApiInterfaceApi` namespace：ApiInterface / ApiInterfaceListParams / ImportResult 类型
- `getApiInterfaceListApi()` — GET 分页查询
- `importApiInterfaceApi()` — POST FormData 上传
- `deleteApiInterfaceApi()` — DELETE 批量删除

#### 2. 视图层
- `views/system/api-interface/index.vue` — 主页面：上传导入按钮（Upload 组件，仅接受 .yaml/.yml）+ 批量删除按钮 + vxe-grid 表格
- `views/system/api-interface/data.ts` — 列定义（checkbox/seq/service/tag/method/path/summary/createdAt/operation[delete]）+ 搜索表单（service/tag/method/path/summary 模糊搜索）

#### 3. 路由 — `router/modules/system.ts`
- 新增 ApiInterface 子路由 `/api-interface`，图标 `lucide:list-restart`

#### 4. 国际化
- `locales/langs/zh-CN/system.json` — 新增 `api_interface` 模块文案（标题、字段名、导入提示、导入结果等）
- `locales/langs/zh-CN/page.json` — 新增 `system.apiInterface` 页面标题

#### 5. 验证
- `pnpm check` 通过（api-interface 相关文件无 type 错误）

### 关键知识点
- **前端上传 YAML 文件**：使用 ant-design-vue 的 `Upload` 组件，`before-upload` 中校验文件后缀 `.yaml/.yml`，构建 `FormData` 发送 POST 请求
- **后端 proto 接收文件**：`ImportApiInterfaceRequest` 使用 `bytes file` 字段，HTTP body 透传
- **导入结果展示**：后端返回 `{total, imported, skipped}`，前端格式化显示"导入成功，已导入 X 个，跳过 Y 个"

### 根因
`NewOpenAPIScanner` 在 `data.go` 中使用硬编码相对路径 `api/gen/openapi/openapi.yaml`，运行时工作目录通常是 `cmd/service/` 或 Docker 镜像内的 `/app`，该路径找不到文件，且错误被静默吞掉（只记日志不阻塞启动），导致 `Metadata()` 返回空 slice。

---

## 修复 multipart/form-data 上传报错 — 方案 B（自定义 handler）

### 问题
`openapi` 导入时前端报错 `unregister Content-Type: multipart/form-data`，原因是 Kratos 的 protobuf HTTP codec 不支持 `multipart/form-data` 的 Content-Type。

### 方案选择
用户选择了 **方案 B**：仿照 `handler/organization.go` 的 `ImportPosition` 模式，在 HTTP server 中注册自定义 handler 处理 multipart 上传。

### 修改清单

#### 1. 修改 `api/gen/go/admin/service/v1/i_system_http.pb.go`（手动编辑）
- 删除 `RegisterSystemHTTPServer` 中的 `r.POST("/admin/v1/api-interface/import", ...)` 行（第 40 行）
- 删除对应的 `_System_ImportApiInterface0_HTTP_Handler` 函数
- 原因：不让 proto 生成的路由注册，避免与 handler 注册的路由冲突

#### 2. 新建 `handler/system.go`（参考 `organization.go` 模式）
- `SystemHandler` 结构体，依赖 `*biz.ApiInterfaceUsecase`
- `ImportApiInterface(ctx http.Context) error` 方法：
  - `req.ParseMultipartForm(32 << 20)` 解析 multipart
  - `req.FormFile("file")` 获取上传文件
  - 校验文件大小（≤ 10MB）和扩展名（.yaml/.yml）
  - `io.ReadAll(file)` 读取文件内容
  - `biz.ParseOpenAPIFile(dataBytes)` 解析 YAML
  - `h.apiInterfaceUc.Import(ctx, items)` 导入
  - `ctx.Result(200, result)` 返回 JSON

#### 3. 新建 `handler/pb/i_system_handler_http.pb.go`（已存在）
- `RegisterSystemHandlerServer` 注册 `POST /admin/v1/api-interface/import` 路由
- 确保 `handler.SystemHandler` 实现 `SystemHandlerHTTPServer` 接口

#### 4. 移动 `ParseOpenAPIFile` 从 data 到 biz 层
- 新建 `biz/openapi_importer.go`（从 `data/openapi_importer.go` 移植）
- 删除 `data/openapi_importer.go`
- 更新 `service/system.go` 的 import 引用（`data.ParseOpenAPIFile` → `biz.ParseOpenAPIFile`）
- 原因：handler 不应直接依赖 data 包，遵循分层架构

#### 5. 更新 `http.go`
- 已注入 `systemHandler *handler.SystemHandler` 参数
- 已调用 `pb.RegisterSystemHandlerServer(srv, systemHandler)`

#### 6. 更新 `handler/handler.go` ProviderSet
- 已加入 `NewSystemHandler`

### 验证
- `go build ./app/admin/service/...` 通过

### 修改清单

1. **`conf.proto`** — 新增 `OpenAPI` message 和 `Bootstrap` 字段 `openapi = 6`
2. **`conf.pb.go`** — 重新 `protoc` 生成，含 `OpenAPI` 结构体 + `GetPath()` 方法
3. **`data.go`** — `NewOpenAPIScanner` 改为接收 `*conf.OpenAPI` 参数；新增 `resolveOpenAPIPath()` 多路径 fallback（配置路径 → `api/gen/openapi/openapi.yaml` → `../api/gen/openapi/openapi.yaml` → `../../api/gen/openapi/openapi.yaml` → `/app/api/gen/openapi/openapi.yaml`）
4. **`wire.go`** — 函数签名增加 `*conf.OpenAPI` 参数
5. **`wire_gen.go`** — 手动同步：函数签名 + 调用 `NewOpenAPIScanner(openapi, logger)`
6. **`main.go`** — `wireApp()` 调用传入 `bc.Openapi`
7. **`config.yaml`** — 新增 `openapi.path: ""`（空=自动 fallback）
8. **`Dockerfile`** — 多阶段构建复制 `api/gen/openapi/openapi.yaml` 到 `/app/api/gen/openapi/`
9. **`scanner.go`** — 删除未使用的 `NewOpenAPIScanner()` 函数（死代码）

### 验证
- `go build ./...` 通过
