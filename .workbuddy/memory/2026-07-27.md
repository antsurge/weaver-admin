# 2026-07-27 工作日志

## 修复表单项 description 导致输入框不对齐问题

### 问题描述
表单项配置了 `description` 后，输入框位置向上偏移。一行两个字段（col-span-1）时，
有 description 和没有 description 的字段输入框顶部不对齐。

### 根因分析
Vben Form 的 `FormItem`（`packages/@core/ui-kit/form-ui/src/form-render/form-field.vue`）结构：

```
FormItem (flex-row + items-center)   ← label 与右侧内容区垂直居中对齐
  ├── FormLabel
  └── div.flex-auto (右侧内容区)
        ├── input wrapper (FormControl + suffix)
        ├── FormDescription            ← 占据文档流空间，使右侧内容区变高
        └── FormMessage (absolute)     ← 错误消息，绝对定位不占空间
```

- 无 description 的字段：右侧内容区高度 = 输入框高度，label 与输入框垂直居中 ✅
- 有 description 的字段：右侧内容区高度 = 输入框 + description，
  `items-center` 使整个内容区居中，输入框相对位置上移 ❌

### 修复方案
在应用层新增全局样式 `frontend/apps/web-antd/src/styles/form.css`，
让 description 绝对定位（与错误消息 FormMessage 相同的处理方式），脱离文档流：

```css
/* description 的 id 由 useFormField 生成：${useId()}-form-item-description */
[id$='-form-item-description'] {
  position: absolute;
}

/* 校验失败时错误消息与 description 位置重叠，优先展示错误消息 */
.form-valid-error [id$='-form-item-description'] {
  visibility: hidden;
}
```

absolute 不指定 top/left 时元素停留在原静态位置（输入框下方），
FormItem 自带的 `pb-4` 底部 padding 已预留空间，不会与下一行重叠。

### 修改的文件
1. **新建** `frontend/apps/web-antd/src/styles/form.css` - 全局修复样式
2. **修改** `frontend/apps/web-antd/src/bootstrap.ts` - 引入 `./styles/form.css`（放在 @vben/styles 之后）

### 关键知识点
**Vben Form FormItem 内部结构（form-field.vue）：**
- 非垂直模式：`flex-row items-center`，label 固定宽度 + 右侧 flex-auto 内容区
- FormDescription 的 id 格式：`${useId()}-form-item-description`，可用属性选择器定位
- FormMessage 本身就是 `absolute` 定位（静态位置 = 输入框下方），FormItem 的 `pb-4` 为其预留空间
- compact 模式下错误消息走 VbenTooltip，不渲染 absolute FormMessage

## 实现 badgeType 清空时 badgeContent 联动清空并禁用

### 需求
菜单管理表单中，当 `badgeType`（徽标类型）未选择或被清空时，
`badgeContent`（徽标内容）需要自动清空值并禁用输入。

### 实现方案
利用 Vben Form `dependencies` 的三个能力组合（`form-render/dependencies.ts`）：
1. `triggerFields` 加入 `'badgeType'`，监听其变化
2. `disabled: (values) => !values.badgeType` - 动态禁用
3. `trigger: (values, formApi) => {...}` - 回调中通过 `formApi.setFieldValue()` 清空值

```typescript
dependencies: {
  triggerFields: ['type', 'badgeType'],
  show: (values) => values.type !== PermissionTypeOptionsValueAction,
  disabled: (values) => !values.badgeType,
  trigger: (values, formApi) => {
    if (!values.badgeType && values.badgeContent) {
      formApi.setFieldValue('badgeContent', undefined);
    }
  },
}
```

### 关键知识点
**Vben Form dependencies 完整能力（dependencies.ts）：**
- `if` - 不渲染 DOM（优先级最高，为 false 时后续逻辑不执行）
- `show` - 隐藏但保留 DOM
- `disabled` - 动态禁用
- `required` - 动态必填
- `componentProps` - 动态组件属性
- `rules` - 动态校验规则
- `trigger(values, formApi)` - 自定义回调，可操作 formApi（setFieldValue 等）
- 所有回调签名：`(formValues, formApi) => any`，支持 async

### 修改的文件
- `frontend/apps/web-antd/src/views/permission/menu/modules/form/index.vue` (badgeContent 字段)

### 后续调整
用户进一步要求：**只有 badgeType 选择 `dot` 时 badgeContent 才能输入**（而非仅判断非空）。
- 条件改为 `values.badgeType !== PermissionBadgeTypeOptionsValueDot`
- 从 `#/views/permission/menu/data` 导入 `PermissionBadgeTypeOptionsValueDot` 常量（值为 `"dot"`）
- badgeType 选项：`dot`（圆点）/ `text`（文本），常量在 `views/permission/menu/data.ts` 定义
- 注：之后用户又改为选择 `text` 时才能输入（text 徽标才需要内容），常量改为 PermissionBadgeTypeOptionsValueText

## 按前端字段重构 menu.proto 的 Create/UpdateMenuRequest

### 需求
前端菜单表单字段和验证已重构，后端 `api/proto/permission/service/v1/menu.proto` 的
`CreateMenuRequest` 和 `UpdateMenuRequest` 需同步匹配。

### 前端字段 → proto 映射
| 前端字段 | 验证(rules.ts) | proto 处理 |
|---------|---------------|-----------|
| name | 2-30 必填 | 验证 64 → 2-30 |
| title | 2-30 必填 | **新增** |
| type | 必选(默认catalog) | 加必填验证 |
| parentID | max 36 | 保持 |
| path | 2-100 | 256 → 100 |
| icon | max 128 | 保持 |
| linkUrl | 2-100 | `url` 重命名为 `linkUrl`，256 → 100 |
| component | 2-100 | 256 → 100 |
| authCode | 2-100 | **新增** |
| badgeType/badgeStyle | - | **新增** |
| badgeContent | max 10 | **新增** |
| remark | - | **新增** |
| ~~code/description~~ | 前端已移除 | `reserved` 保留编号和名称 |

### 关键决策
1. **code/description 用 reserved 移除**：前端表单已无此字段（备注用 remark），
   用 `reserved 2, 3; reserved "code", "description";` 防止编号被误复用（proto3 最佳实践）
2. **url → linkUrl 重命名**：保持字段编号不变（二进制兼容），JSON key 变化匹配前端
3. **可选字段只限 max**：path/component/linkUrl/authCode 等条件显示字段，
   proto 平面结构无法表达"显示时必填"，只限制最大长度，必填由前端保证
4. **name/title 必填**：CEL `this.size() >= 2 && this.size() <= 30`
5. **Menu message 不动**：数据模型字段（含 code/description）保持不变，只改请求 message

### 验证
- `buf build --path proto/permission/service/v1/menu.proto` 零错误
- lint 报的 camelCase 警告为项目既有风格（与前端 JSON 字段名对齐），保留

### 修改的文件
- `api/proto/permission/service/v1/menu.proto` (CreateMenuRequest, UpdateMenuRequest)

### 待办（提醒用户）
1. 重新生成代码：`buf generate` 或项目对应的 make 命令（gen/go、openapi.yaml）
2. 后端 `biz.Menu`（app/admin/service/internal/biz/menu.go）目前只有 Url 无 LinkUrl，
   也缺 Title/AuthCode/BadgeType/BadgeContent/BadgeStyle 字段，后续需同步
3. service 层 `copier.Copy(&input, &req)` 依赖字段名匹配

## biz/ent/data 三层同步修改（已完成）

### 修改内容

**1. `app/admin/service/internal/biz/menu.go` - Menu 结构体**
- `Url` → `LinkUrl`（json: linkUrl，与 proto 生成代码字段名匹配）
- 新增：`Title`、`AuthCode`、`BadgeType`、`BadgeContent`、`BadgeStyle`
- service 层 copier.Copy 按字段名自动匹配，无需改 service

**2. `app/admin/service/internal/data/ent/schema/menu.go` - ent schema**
- `code`：去掉 `NotEmpty().Unique()` → `Default("")`（前端不再提交 code；
  不能用 Optional+Nillable+Unique，改动链长；空字符串无唯一约束后可重复，最安全）
- `type` 枚举：`("menu_dir","menu","button")` → `("menu_dir","catalog","menu","iframe","link","action","button")`
  （保留旧值 menu_dir/button 兼容存量数据；前端实际值 catalog/menu/iframe/link/action）
- 新增列：`title`(60)、`auth_code`(100)、`badge_type`(20)、`badge_content`(10)、`badge_style`(20)
- `url` 列名保留不动（数据库零迁移），data 层手动映射 `LinkUrl ↔ URL`
- 注意：前端 type 值是 **action**（不是 button）、**iframe**（不是 embedded）—— proto 注释已同步修正

**3. `app/admin/service/internal/data/menu.go` - repo 映射**
- Create/Update：新增 SetTitle/SetAuthCode/SetBadgeType/SetBadgeContent/SetBadgeStyle，
  启用原被注释的 `SetURL(p.LinkUrl)`
- ListMenu/GetMenusByIDs 读取映射同步新字段

**4. `app/admin/service/internal/data/role.go` - 角色关联菜单映射**
- 同样构造 biz.Menu，同步 `Url` → `LinkUrl` 及新字段（遗漏会编译错误）

### 验证
- `go run entgo.io/ent/cmd/ent generate ./schema` 成功（已顺手重新生成 ent 代码）
- `go build ./...`（app/admin/service）全部通过
- 注意：本地 go1.22 需 `GOTOOLCHAIN=auto`（项目要求 go>=1.24.6）

### 用户待办
1. 重新生成 proto 代码：`buf generate`（pb.go、openapi.yaml）—— ent 代码已生成
2. 数据库迁移：ent 启动自动加新列、code 唯一索引移除、type 枚举扩展

## 修复菜单编辑时 title 国际化翻译不显示

### 问题描述
菜单管理点开编辑时，`title` 字段的 addonAfter（国际化翻译值）不显示，
只有手动修改 title 输入框内容后才会显示。

### 根因
后端返回的 `Menu` 数据中 `title` 是**顶级字段**（proto field 13 / ent `title` 列），
但 `onOpenChange` 中从 `data.meta?.title` 读取（前端 TS 类型 `PermissionMenu.meta.title` 误导），
导致 `titleSuffix` 被初始化为空字符串，addonAfter 不显示。

而 `onChange` 从输入框的 `value` 读取（顶级字段值），所以修改后能正常显示。

### 修复
`frontend/apps/web-antd/src/views/permission/menu/modules/form/index.vue`（onOpenChange）：
- `data.meta?.title` → `data.title`
- 加 `$te` 判断（与 onChange 逻辑一致）

### 注意
前端 `PermissionMenuApi.PermissionMenu` 类型定义中 title 在 `meta.title` 下（Vben 原始路由类型），
但后端实际返回顶级 title。列表页 `index.vue` 的 `#title` slot 中 `row.meta?.title` / `row.meta?.icon`
可能也存在同样问题，待确认。
3. 存量数据：menu_dir/button 仍兼容；如需统一可后续 SQL 洗数据为 catalog/action
