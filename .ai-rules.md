# AI Coding Rules（强制执行）

## 一、技术栈（必须遵守）
- 后端：Go + Kratos
- ORM：Ent（禁止手写 SQL）
- API：Proto（bapis 仓库统一管理）
- 前端：Vue3 + Vben Admin
- 请求库：统一 useRequest / vben-request

---

## 二、命名规范

### 1. 通用
- 变量：camelCase
- 常量：UPPER_SNAKE_CASE
- 文件名：snake_case

### 2. 数据库（Ent）
- 表名：snake_case（复数可选，统一即可）
- 字段：snake_case
- 主键：id（string，UUID）

### 3. 枚举（非常重要）
- 值：全大写 + 下划线
    - PRICING
    - PRICE_ADJUSTMENT
- Proto 枚举必须带前缀：
    - PRICE_OPERATION_TYPE_PRICING

---

## 三、API 规范（Proto）

### 1. 必须使用 Proto 定义接口
- 禁止直接写 REST JSON 接口
- 所有接口必须在 bapis 仓库定义

### 2. 命名规范
- ListXXX（分页查询）
- GetXXX（详情）
- CreateXXX（创建）
- UpdateXXX（更新）
- DeleteXXX（删除）

### 3. 分页必须统一
```proto
message PageRequest {
  int32 page = 1;
  int32 page_size = 2;
}

message PageResponse {
  int32 total = 1;
}

